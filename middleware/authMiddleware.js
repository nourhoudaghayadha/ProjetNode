const jwt = require('jsonwebtoken');

exports.authenticateUser = (req, res, next) => {
    // Middleware d'authentification JWT ici
};

exports.authorizeAdmin = (req, res, next) => {
helllllllllllllo};
